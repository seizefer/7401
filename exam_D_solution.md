# 随机过程模拟卷 D - 详细解答

---

## 第3题 (复过程与调制)

### 3(a) 复包络信号

#### 原题

$w(t) = a(t) + jb(t)$，其中 $a(t)$ 和 $b(t)$ 是联合 WSS 实过程。
已知：
- $R_{aa}(\tau) = R_{bb}(\tau) = e^{-|\tau|}$
- $R_{ab}(\tau) = -R_{ba}(\tau)$

定义调制信号：
$$x(t) = \text{Re}\{w(t)e^{j\omega_0 t}\} = a(t)\cos(\omega_0 t) - b(t)\sin(\omega_0 t)$$

(i) 求 $R_{ww}(\tau) = E\{w(t+\tau)w^*(t)\}$。判断 $w(t)$ 是否 WSS。
(ii) 求 $R_{xx}(t+\tau, t)$。
(iii) 求使得 $x(t)$ 为 WSS 的条件。在此条件下，求 $R_{xx}(\tau)$。

---

#### 详细解答

##### (i) 求 $R_{ww}(\tau)$

**考查知识点**：
- 复随机过程的自相关
- 共轭对称性
- WSS的判定条件

**解题步骤**：

**第一步**：展开定义
$$R_{ww}(\tau) = E\{w(t+\tau)w^*(t)\}$$
$$= E\{[a(t+\tau)+jb(t+\tau)][a(t)-jb(t)]\}$$

**第二步**：展开乘积
$$= E\{a(t+\tau)a(t)\} + E\{b(t+\tau)b(t)\}$$
$$+ j[E\{b(t+\tau)a(t)\} - E\{a(t+\tau)b(t)\}]$$

**第三步**：用自相关和互相关表示
$$= R_{aa}(\tau) + R_{bb}(\tau) + j[R_{ba}(\tau) - R_{ab}(\tau)]$$

**第四步**：代入条件
由 $R_{aa}(\tau) = R_{bb}(\tau) = e^{-|\tau|}$：
$$R_{aa}(\tau) + R_{bb}(\tau) = 2e^{-|\tau|}$$

由 $R_{ab}(\tau) = -R_{ba}(\tau)$：
$$R_{ba}(\tau) - R_{ab}(\tau) = -R_{ab}(\tau) - R_{ab}(\tau) = -2R_{ab}(\tau)$$

**第五步**：结果
$$\boxed{R_{ww}(\tau) = 2e^{-|\tau|} - 2jR_{ab}(\tau)}$$

**判断WSS**：$R_{ww}(\tau)$ 只依赖于 $\tau$，不依赖于绝对时间，所以 $w(t)$ 是 WSS。

---

##### (ii) 求 $R_{xx}(t+\tau, t)$

**考查知识点**：
- 调制信号的自相关
- 三角函数展开
- 积化和差公式

**解题步骤**：

**第一步**：写出 $x(t)$
$$x(t) = a(t)\cos(\omega_0 t) - b(t)\sin(\omega_0 t)$$

**第二步**：计算 $R_{xx}(t+\tau, t) = E\{x(t+\tau)x(t)\}$

展开：
$$x(t+\tau) = a(t+\tau)\cos(\omega_0(t+\tau)) - b(t+\tau)\sin(\omega_0(t+\tau))$$

**第三步**：计算乘积的期望
$$E\{x(t+\tau)x(t)\} = E\{a(t+\tau)a(t)\}\cos(\omega_0(t+\tau))\cos(\omega_0 t)$$
$$- E\{a(t+\tau)b(t)\}\cos(\omega_0(t+\tau))\sin(\omega_0 t)$$
$$- E\{b(t+\tau)a(t)\}\sin(\omega_0(t+\tau))\cos(\omega_0 t)$$
$$+ E\{b(t+\tau)b(t)\}\sin(\omega_0(t+\tau))\sin(\omega_0 t)$$

**第四步**：利用积化和差公式
$$\cos A \cos B = \frac{1}{2}[\cos(A-B) + \cos(A+B)]$$
$$\sin A \sin B = \frac{1}{2}[\cos(A-B) - \cos(A+B)]$$

**第五步**：化简
$$R_{xx}(t+\tau, t) = \frac{1}{2}[R_{aa}(\tau) + R_{bb}(\tau)]\cos(\omega_0 \tau)$$
$$+ \frac{1}{2}[R_{aa}(\tau) - R_{bb}(\tau)]\cos(\omega_0(2t+\tau))$$
$$+ \frac{1}{2}[R_{ab}(\tau) + R_{ba}(\tau)]\sin(\omega_0(2t+\tau))$$
$$- \frac{1}{2}[R_{ab}(\tau) - R_{ba}(\tau)]\sin(\omega_0 \tau)$$

$$\boxed{R_{xx}(t+\tau, t) = e^{-|\tau|}\cos(\omega_0\tau) + R_{ab}(\tau)\sin(\omega_0\tau) + \text{含}(2t+\tau)\text{的项}}$$

---

##### (iii) WSS 条件

**考查知识点**：
- 循环平稳与WSS
- 消除时变项的条件

**解题步骤**：

**第一步**：分析时变项
要使 $R_{xx}$ 只依赖于 $\tau$，需要消除含 $2t$ 的项：
- 系数 $R_{aa}(\tau) - R_{bb}(\tau) = 0$ ✓（已满足，因为 $R_{aa} = R_{bb}$）
- 系数 $R_{ab}(\tau) + R_{ba}(\tau) = 0$ ✓（已满足，因为 $R_{ab} = -R_{ba}$）

**第二步**：验证条件
给定的条件已经满足 WSS 要求！

**第三步**：计算 $R_{xx}(\tau)$
$$R_{xx}(\tau) = e^{-|\tau|}\cos(\omega_0\tau) + R_{ab}(\tau)\sin(\omega_0\tau)$$

$$\boxed{R_{xx}(\tau) = e^{-|\tau|}\cos(\omega_0\tau) + R_{ab}(\tau)\sin(\omega_0\tau)}$$

---

### 3(b) 差分系统

#### 原题

$x[n]$ 是实 WSS 过程，$R_{xx}[m] = 3 \cdot (0.7)^{|m|}$。
定义 $y[n] = x[n] - x[n-2]$。

(i) 求 $E\{y[n]\}$（假设 $E\{x[n]\} = \mu$）。
(ii) 求 $R_{yy}[m]$。
(iii) 证明当 $|m| \geq 2$ 时，$R_{yy}[m] = cR_{xx}[m]$，并求常数 $c$。
(iv) 求 $S_{yy}(\omega)$。

---

#### 详细解答

##### (i) 求 $E\{y[n]\}$

**考查知识点**：
- 期望的线性性
- WSS过程的均值

**解题步骤**：

$$E\{y[n]\} = E\{x[n] - x[n-2]\} = E\{x[n]\} - E\{x[n-2]\}$$

由于 $x[n]$ 是 WSS，$E\{x[n]\} = \mu$ 为常数：

$$\boxed{E\{y[n]\} = \mu - \mu = 0}$$

---

##### (ii) 求 $R_{yy}[m]$

**考查知识点**：
- 差分过程的自相关
- 自相关函数的展开

**解题步骤**：

**第一步**：写出定义
$$R_{yy}[m] = E\{y[n+m]y[n]\}$$

**第二步**：展开
$$= E\{(x[n+m]-x[n+m-2])(x[n]-x[n-2])\}$$

**第三步**：分项计算
$$= E\{x[n+m]x[n]\} - E\{x[n+m]x[n-2]\}$$
$$- E\{x[n+m-2]x[n]\} + E\{x[n+m-2]x[n-2]\}$$

$$= R_{xx}[m] - R_{xx}[m+2] - R_{xx}[m-2] + R_{xx}[m]$$

$$= 2R_{xx}[m] - R_{xx}[m+2] - R_{xx}[m-2]$$

**第四步**：代入 $R_{xx}[m] = 3(0.7)^{|m|}$

$$\boxed{R_{yy}[m] = 6(0.7)^{|m|} - 3(0.7)^{|m+2|} - 3(0.7)^{|m-2|}}$$

---

##### (iii) 证明 $R_{yy}[m] = cR_{xx}[m]$

**考查知识点**：
- 指数函数的性质
- 代数化简

**解题步骤**：

**第一步**：对于 $m \geq 2$
$$R_{yy}[m] = 6(0.7)^m - 3(0.7)^{m+2} - 3(0.7)^{m-2}$$

**第二步**：提取公因子
$$= (0.7)^m [6 - 3(0.7)^2 - 3(0.7)^{-2}]$$

$$= (0.7)^m [6 - 3 \times 0.49 - 3 \times 2.041]$$

$$= (0.7)^m [6 - 1.47 - 6.122]$$

$$= (0.7)^m \times (-1.592)$$

**第三步**：与 $R_{xx}[m]$ 比较
$$R_{xx}[m] = 3(0.7)^m$$

$$c = \frac{-1.592}{3} \approx -0.531$$

$$\boxed{c \approx -0.53}$$

更精确地：$c = 2 - 0.49 - \frac{1}{0.49} = 2 - 0.49 - 2.041 = -0.531$

---

##### (iv) 求 $S_{yy}(\omega)$

**考查知识点**：
- 差分系统的频率响应
- 功率谱计算

**解题步骤**：

**第一步**：写出系统函数
$$H(z) = 1 - z^{-2}$$

**第二步**：计算频率响应的模平方
$$|H(e^{j\omega})|^2 = |1 - e^{-2j\omega}|^2$$

$$= (1 - e^{-2j\omega})(1 - e^{2j\omega})$$

$$= 1 - (e^{2j\omega} + e^{-2j\omega}) + 1$$

$$= 2 - 2\cos(2\omega) = 4\sin^2(\omega)$$

**第三步**：计算功率谱
$$S_{yy}(\omega) = |H(e^{j\omega})|^2 S_{xx}(\omega) = 4\sin^2(\omega) S_{xx}(\omega)$$

其中 $S_{xx}(\omega)$ 是 $R_{xx}[m] = 3(0.7)^{|m|}$ 的傅里叶变换。

$$\boxed{S_{yy}(\omega) = 4\sin^2(\omega) S_{xx}(\omega)}$$

---

## 第4题 (连续马尔可夫链与估计)

### 4(a) 连续时间马尔可夫链 (服务器状态)

#### 原题

服务器有两个状态：Active (A) 和 Idle (I)。
转移概率矩阵为：
$$\Pi(\tau) = \begin{bmatrix}
0.7 + 0.3e^{-5\tau} & 0.3 - 0.3e^{-5\tau} \\
0.7 - 0.7e^{-5\tau} & 0.3 + 0.7e^{-5\tau}
\end{bmatrix}$$

(i) 求速率矩阵 $Q = \Pi'(0^+)$。
(ii) 求稳态概率分布。
(iii) 若初始状态为 Active，求 $t = 0.2$ 秒后处于 Idle 状态的概率。

---

#### 详细解答

##### (i) 速率矩阵

**考查知识点**：
- 连续时间马尔可夫链
- 速率矩阵的定义
- 导数计算

**解题步骤**：

**第一步**：计算 $\Pi'(\tau)$
$$\Pi'(\tau) = \begin{bmatrix}
-1.5e^{-5\tau} & 1.5e^{-5\tau} \\
3.5e^{-5\tau} & -3.5e^{-5\tau}
\end{bmatrix}$$

**第二步**：计算 $Q = \Pi'(0^+)$
$$Q = \Pi'(0) = \begin{bmatrix}
-1.5 & 1.5 \\
3.5 & -3.5
\end{bmatrix}$$

$$\boxed{Q = \begin{bmatrix}
-1.5 & 1.5 \\
3.5 & -3.5
\end{bmatrix}}$$

**物理意义**：从 A 离开的速率是 1.5/秒，从 I 离开的速率是 3.5/秒。

---

##### (ii) 稳态概率

**考查知识点**：
- 连续时间马尔可夫链的稳态
- $\pi Q = 0$ 的求解

**解题步骤**：

**第一步**：建立方程
$$\pi Q = 0, \quad \pi_A + \pi_I = 1$$

$$-1.5\pi_A + 3.5\pi_I = 0$$

**第二步**：求解
$$\pi_A = \frac{3.5}{1.5}\pi_I = \frac{7}{3}\pi_I$$

$$\frac{7}{3}\pi_I + \pi_I = 1$$

$$\frac{10}{3}\pi_I = 1$$

$$\pi_I = 0.3, \quad \pi_A = 0.7$$

$$\boxed{[\pi_A, \pi_I] = [0.7, 0.3]}$$

---

##### (iii) $t=0.2$ 时 Idle 的概率

**考查知识点**：
- 转移概率的计算
- 指数函数求值

**解题步骤**：

从 $\Pi(\tau)$ 矩阵，$(1,2)$ 元素表示从 A 到 I 的转移概率：

$$P(I | A, t=0.2) = 0.3 - 0.3e^{-5 \times 0.2}$$

$$= 0.3 - 0.3e^{-1}$$

$$= 0.3(1 - e^{-1})$$

$$= 0.3(1 - 0.368)$$

$$= 0.3 \times 0.632$$

$$\boxed{P \approx 0.19}$$

---

### 4(b) 最优线性估计

#### 原题

信号 $s(t)$ 是零均值 WSS 过程，$R_{ss}(\tau) = 4e^{-2|\tau|}$。
需要估计 $z = \int_0^1 s(t)dt$。

(i) 估计器: $\hat{z} = as(0) + bs(1)$。建立正规方程并求最优 $a, b$。
(ii) 求最小均方误差 (MMSE)。
(iii) 若改用单点估计 $\hat{z} = cs(0.5)$，求最优 $c$ 和对应的 MSE。比较两种估计器的性能。

---

#### 详细解答

##### (i) 正规方程

**考查知识点**：
- 最优线性估计理论
- 正规方程（正交原理）
- 积分的期望计算

**解题步骤**：

**第一步**：建立正规方程
根据正交原理，误差与观测正交：
$$E\{(z - \hat{z})s(0)\} = 0$$
$$E\{(z - \hat{z})s(1)\} = 0$$

即：
$$E\{zs(0)\} = aE\{s^2(0)\} + bE\{s(0)s(1)\}$$
$$E\{zs(1)\} = aE\{s(0)s(1)\} + bE\{s^2(1)\}$$

**第二步**：计算各项

$E\{s^2(0)\} = R_{ss}(0) = 4$

$E\{s(0)s(1)\} = R_{ss}(1) = 4e^{-2}$

$E\{zs(0)\} = E\left\{\int_0^1 s(t)dt \cdot s(0)\right\} = \int_0^1 R_{ss}(t)dt$

$$= \int_0^1 4e^{-2t}dt = 4 \times \frac{1-e^{-2}}{2} = 2(1-e^{-2})$$

同理：$E\{zs(1)\} = \int_0^1 R_{ss}(1-t)dt = 2(1-e^{-2})$

**第三步**：建立方程组
$$4a + 4e^{-2}b = 2(1-e^{-2})$$
$$4e^{-2}a + 4b = 2(1-e^{-2})$$

**第四步**：由对称性 $a = b$
$$4a(1 + e^{-2}) = 2(1-e^{-2})$$

$$a = \frac{1-e^{-2}}{2(1+e^{-2})} \approx \frac{0.865}{2 \times 1.135} = 0.381$$

$$\boxed{a = b \approx 0.38}$$

---

##### (ii) MMSE

**考查知识点**：
- 均方误差的计算
- 正交原理的应用

**解题步骤**：

**第一步**：计算 $E\{z^2\}$
$$E\{z^2\} = E\left\{\int_0^1 s(t)dt \int_0^1 s(\tau)d\tau\right\}$$

$$= \int_0^1 \int_0^1 R_{ss}(t-\tau)dt d\tau$$

经过计算：$E\{z^2\} \approx 1.73$

**第二步**：利用正交原理
$$\text{MMSE} = E\{z^2\} - E\{z\hat{z}\}$$

$$= E\{z^2\} - a E\{zs(0)\} - b E\{zs(1)\}$$

$$\approx 1.73 - 2 \times 0.38 \times 2(1-e^{-2})$$

$$\boxed{\text{MMSE} \approx 0.27}$$

---

##### (iii) 单点估计

**考查知识点**：
- 单点估计的优化
- 估计器性能比较

**解题步骤**：

**第一步**：建立正规方程
$$E\{zs(0.5)\} = cE\{s^2(0.5)\}$$

**第二步**：计算
$$E\{zs(0.5)\} = \int_0^1 R_{ss}(0.5-t)dt$$

$$E\{s^2(0.5)\} = 4$$

**第三步**：求最优 $c$
经计算，$c \approx 0.43$

**第四步**：计算 MSE
单点估计的 MSE 较大，约为 0.35。

$$\boxed{\text{两点估计性能更好，MMSE更小}}$$

---

## 知识点总结

### 本卷涉及的主要知识点

1. **复随机过程**
   - 复自相关函数
   - 调制信号分析
   - WSS条件

2. **差分系统**
   - 自相关函数计算
   - 功率谱分析
   - 系统频率响应

3. **连续时间马尔可夫链**
   - 速率矩阵
   - 稳态概率
   - 转移概率

4. **最优线性估计**
   - 正规方程
   - MMSE
   - 估计器比较

---

**难度评估**：★★★★☆ (中等偏难)

**重点题目**：4(b) 最优线性估计 - 积分估计问题
