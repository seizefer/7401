# 随机过程模拟卷 A - 详细解答

---

## 第3题 (非平稳过程与功率谱)

### 3(a) 股票价格模型

#### 原题

Mr. Bull Market 的股票收益建模为实非平稳过程 $x(t)$ ($t \geq 0$)，且 $x(0)=0$。满足以下性质：

1. 非重叠区间的增量不相关：
$$E\left([x(t_4)-x(t_3)][x(t_2)-x(t_1)]\right)=0 \quad \text{for any } 0 \leq t_1 \leq t_2 \leq t_3 \leq t_4.$$

2. 增量平方的期望与时间间隔长度成正比：
$$E\left([x(t_2)-x(t_1)]^2\right)=\sigma^2(t_2-t_1) \quad \text{for any } 0 \leq t_1 \leq t_2.$$

求 $R_{xx}(t_1, t_2)$：
(i) 利用性质(1)证明当 $0 \leq t_1 \leq t_2 \leq t_3$ 时，$R_{xx}(t_1, t_3) = R_{xx}(t_1, t_2)$。
(ii) 利用(i)的结果和性质(2)，当 $t_1 \leq t_2$ 时求 $R_{xx}(t_1, t_2)$。
(iii) 综合求出任意 $t_1, t_2$ 下的 $R_{xx}(t_1, t_2)$。

---

#### 详细解答

##### (i) 证明 $R_{xx}(t_1, t_3) = R_{xx}(t_1, t_2)$

**考查知识点**：
- 自相关函数的定义
- 增量过程的性质
- 期望的线性性

**解题步骤**：

**第一步**：写出自相关函数的定义
$$R_{xx}(t_1, t_3) = E\{x(t_1) \cdot x(t_3)\}$$

**第二步**：将 $x(t_3)$ 分解为增量形式
由于 $x(t_3) = x(t_2) + [x(t_3) - x(t_2)]$，代入得：
$$R_{xx}(t_1, t_3) = E\{x(t_1) \cdot [x(t_2) + (x(t_3) - x(t_2))]\}$$

**第三步**：利用期望的线性性展开
$$= E\{x(t_1) \cdot x(t_2)\} + E\{x(t_1) \cdot [x(t_3) - x(t_2)]\}$$

**第四步**：分析第二项
由于 $x(0) = 0$，所以 $x(t_1) = x(t_1) - x(0)$，这是区间 $[0, t_1]$ 上的增量。

根据性质(1)，当 $0 \leq t_1 \leq t_2 \leq t_3$ 时：
$$E\{[x(t_1) - x(0)] \cdot [x(t_3) - x(t_2)]\} = 0$$

即：
$$E\{x(t_1) \cdot [x(t_3) - x(t_2)]\} = 0$$

**第五步**：得出结论
$$R_{xx}(t_1, t_3) = E\{x(t_1) \cdot x(t_2)\} + 0 = R_{xx}(t_1, t_2)$$

$$\boxed{R_{xx}(t_1, t_3) = R_{xx}(t_1, t_2) \quad \text{当 } 0 \leq t_1 \leq t_2 \leq t_3}$$

---

##### (ii) 求 $R_{xx}(t_1, t_2)$ 当 $t_1 \leq t_2$

**考查知识点**：
- 增量过程的二阶矩性质
- 自相关函数的计算

**解题步骤**：

**第一步**：利用(i)的结论
由(i)可知，当 $t_1 \leq t_2$ 时：
$$R_{xx}(t_1, t_2) = R_{xx}(t_1, t_1)$$

**第二步**：计算 $R_{xx}(t_1, t_1) = E\{x^2(t_1)\}$

利用性质(2)，取 $t_2 = t_1$，$t_1 = 0$：
$$E\{[x(t_1) - x(0)]^2\} = \sigma^2(t_1 - 0) = \sigma^2 t_1$$

**第三步**：展开左边
$$E\{x^2(t_1) - 2x(t_1)x(0) + x^2(0)\} = \sigma^2 t_1$$

由于 $x(0) = 0$：
$$E\{x^2(t_1)\} - 0 + 0 = \sigma^2 t_1$$

**第四步**：得出结论
$$R_{xx}(t_1, t_1) = \sigma^2 t_1$$

因此：
$$\boxed{R_{xx}(t_1, t_2) = \sigma^2 t_1 \quad \text{当 } t_1 \leq t_2}$$

---

##### (iii) 任意 $t_1, t_2$ 下的 $R_{xx}(t_1, t_2)$

**考查知识点**：
- 自相关函数的对称性
- 分段函数的表示

**解题步骤**：

**第一步**：利用自相关函数的对称性
对于实随机过程：
$$R_{xx}(t_1, t_2) = E\{x(t_1)x(t_2)\} = E\{x(t_2)x(t_1)\} = R_{xx}(t_2, t_1)$$

**第二步**：分情况讨论
- 当 $t_1 \leq t_2$ 时：$R_{xx}(t_1, t_2) = \sigma^2 t_1$
- 当 $t_1 > t_2$ 时：$R_{xx}(t_1, t_2) = R_{xx}(t_2, t_1) = \sigma^2 t_2$

**第三步**：统一表示
$$\boxed{R_{xx}(t_1, t_2) = \sigma^2 \min(t_1, t_2)}$$

**物理意义**：这正是**维纳过程（布朗运动）**的自相关函数，描述了股票收益的随机积累特性。

---

### 3(b) 信号处理系统

#### 原题

$y(t)$ 是实 WSS 过程，功率谱 $S_{yy}(\omega)$ 未知。
工程师 A 测量 $p(t) = y(t+b) + 2y(t) + y(t-b)$。
工程师 B 测量 $q(t) = y(t+b) - y(t-b)$。

(i) 求 $p(t)$ 的功率谱 $S_{pp}(\omega)$，并证明：
$$S_{pp}(\omega) = 2(1 + \cos(b\omega))^2 S_{yy}(\omega)$$

(ii) 求 $q(t)$ 的功率谱 $S_{qq}(\omega)$。

(iii) 求同一时刻的互相关 $R_{pq}(0)$。

(iv) 若信号 $y(t)$ 的主要频率成分在 $\omega = \pi/(2b)$，比较哪个测量系统对该频率的响应更好。

---

#### 详细解答

##### (i) 证明 $S_{pp}(\omega)$

**考查知识点**：
- LTI系统的频率响应
- 功率谱与频率响应的关系
- 傅里叶变换的时移性质

**解题步骤**：

**第一步**：将 $p(t)$ 表示为 LTI 系统的输出
$p(t) = y(t+b) + 2y(t) + y(t-b)$ 可以看作是输入 $y(t)$ 通过冲激响应为：
$$h(t) = \delta(t+b) + 2\delta(t) + \delta(t-b)$$
的系统。

**第二步**：计算频率响应 $H(\omega)$
$$H(\omega) = \mathcal{F}\{h(t)\} = \int_{-\infty}^{\infty} h(t)e^{-j\omega t}dt$$

$$= e^{j\omega b} + 2 + e^{-j\omega b}$$

利用欧拉公式 $e^{j\theta} + e^{-j\theta} = 2\cos\theta$：
$$H(\omega) = 2 + 2\cos(\omega b) = 2(1 + \cos(\omega b))$$

**第三步**：计算功率谱
$$S_{pp}(\omega) = |H(\omega)|^2 S_{yy}(\omega)$$

$$= [2(1 + \cos(\omega b))]^2 S_{yy}(\omega)$$

$$\boxed{S_{pp}(\omega) = 4(1 + \cos(b\omega))^2 S_{yy}(\omega)}$$

> **注**：题目公式中的系数2应为4。

---

##### (ii) 求 $S_{qq}(\omega)$

**考查知识点**：
- 差分系统的频率响应
- 高通滤波器特性

**解题步骤**：

**第一步**：将 $q(t)$ 表示为 LTI 系统
$q(t) = y(t+b) - y(t-b)$

冲激响应：$g(t) = \delta(t+b) - \delta(t-b)$

**第二步**：计算频率响应
$$G(\omega) = e^{j\omega b} - e^{-j\omega b} = 2j\sin(\omega b)$$

**第三步**：计算功率谱
$$S_{qq}(\omega) = |G(\omega)|^2 S_{yy}(\omega) = |2j\sin(\omega b)|^2 S_{yy}(\omega)$$

$$= 4\sin^2(\omega b) S_{yy}(\omega)$$

$$\boxed{S_{qq}(\omega) = 4\sin^2(b\omega) S_{yy}(\omega)}$$

**物理意义**：$q(t)$ 系统是一个高通滤波器，在 $\omega = 0$ 处响应为零。

---

##### (iii) 求 $R_{pq}(0)$

**考查知识点**：
- 互相关函数的计算
- WSS过程的性质
- 期望的展开

**解题步骤**：

**第一步**：写出定义
$$R_{pq}(0) = E\{p(t)q(t)\}$$

**第二步**：展开 $p(t)$ 和 $q(t)$
$$= E\{[y(t+b) + 2y(t) + y(t-b)][y(t+b) - y(t-b)]\}$$

**第三步**：分项展开
$$= E\{y^2(t+b)\} - E\{y(t+b)y(t-b)\}$$
$$+ 2E\{y(t)y(t+b)\} - 2E\{y(t)y(t-b)\}$$
$$+ E\{y(t-b)y(t+b)\} - E\{y^2(t-b)\}$$

**第四步**：利用 WSS 性质简化
由于 $y(t)$ 是 WSS：
- $E\{y^2(t+b)\} = E\{y^2(t-b)\} = R_{yy}(0)$
- $E\{y(t)y(t+b)\} = R_{yy}(b)$
- $E\{y(t)y(t-b)\} = R_{yy}(-b) = R_{yy}(b)$（实过程）
- $E\{y(t+b)y(t-b)\} = R_{yy}(2b)$

**第五步**：代入计算
$$R_{pq}(0) = R_{yy}(0) - R_{yy}(2b) + 2R_{yy}(b) - 2R_{yy}(b) + R_{yy}(2b) - R_{yy}(0)$$

$$= 0$$

$$\boxed{R_{pq}(0) = 0}$$

**物理意义**：$p(t)$ 和 $q(t)$ 在同一时刻不相关。

---

##### (iv) 频率响应比较

**考查知识点**：
- 滤波器在特定频率的增益
- 系统频率选择特性

**解题步骤**：

**第一步**：计算 $\omega = \pi/(2b)$ 处的响应

对于 $p(t)$ 系统：
$$|H(\pi/(2b))|^2 = 4(1 + \cos(\pi/2))^2 = 4(1+0)^2 = 4$$

对于 $q(t)$ 系统：
$$|G(\pi/(2b))|^2 = 4\sin^2(\pi/2) = 4 \times 1 = 4$$

**第二步**：分析结果
两个系统在目标频率 $\omega = \pi/(2b)$ 处的增益相同。

**第三步**：比较其他特性
- $p(t)$ 系统在 $\omega = 0$ 处：$|H(0)|^2 = 4(1+1)^2 = 16$（低频增益大）
- $q(t)$ 系统在 $\omega = 0$ 处：$|G(0)|^2 = 0$（完全抑制直流）

$$\boxed{\text{两者在 } \omega = \pi/(2b) \text{ 处响应相等；但 } q(t) \text{ 具有高通特性，更适合去除直流分量}}$$

---

## 第4题 (MA过程与马尔可夫链)

### 4(a) MA(2) 过程分析

#### 原题

$x[n]$ 是实离散 WSS 白噪声，自相关为：
$$R_{xx}[m] = q\delta[m]$$

输出 $y[n]$ (MA(2)过程) 满足：
$$y[n] = x[n] + \alpha x[n-1] + \beta x[n-2]$$

(i) 求 $R_{yy}[0]$、$R_{yy}[1]$、$R_{yy}[2]$。
(ii) 当 $|m| > 2$ 时，求 $R_{yy}[m]$。
(iii) 求 $S_{yy}(\omega)$，并证明它是实函数。

---

#### 详细解答

##### (i) 求 $R_{yy}[0]$、$R_{yy}[1]$、$R_{yy}[2]$

**考查知识点**：
- 白噪声的自相关性质
- MA过程的自相关计算
- 期望的展开技巧

**解题步骤**：

**计算 $R_{yy}[0]$**：

**第一步**：写出定义
$$R_{yy}[0] = E\{y^2[n]\}$$

**第二步**：展开 $y[n]$
$$= E\{(x[n] + \alpha x[n-1] + \beta x[n-2])^2\}$$

**第三步**：展开平方
$$= E\{x^2[n] + \alpha^2 x^2[n-1] + \beta^2 x^2[n-2]$$
$$+ 2\alpha x[n]x[n-1] + 2\beta x[n]x[n-2] + 2\alpha\beta x[n-1]x[n-2]\}$$

**第四步**：利用白噪声性质
由于 $E\{x[n]x[m]\} = q\delta[n-m]$，交叉项全为0：

$$R_{yy}[0] = q + \alpha^2 q + \beta^2 q = q(1 + \alpha^2 + \beta^2)$$

---

**计算 $R_{yy}[1]$**：

**第一步**：写出定义
$$R_{yy}[1] = E\{y[n]y[n-1]\}$$

**第二步**：展开两个项
$$= E\{(x[n] + \alpha x[n-1] + \beta x[n-2])(x[n-1] + \alpha x[n-2] + \beta x[n-3])\}$$

**第三步**：找非零交叉项
只有当下标相同时才非零：
- $x[n-1]$ 与 $x[n-1]$：系数为 $\alpha \cdot 1 = \alpha$
- $x[n-2]$ 与 $x[n-2]$：系数为 $\beta \cdot \alpha = \alpha\beta$

$$R_{yy}[1] = \alpha q + \alpha\beta q = q\alpha(1 + \beta)$$

---

**计算 $R_{yy}[2]$**：

**第一步**：写出定义
$$R_{yy}[2] = E\{y[n]y[n-2]\}$$

**第二步**：找非零项
只有 $x[n-2]$ 与 $x[n-2]$ 相关：系数为 $\beta \cdot 1 = \beta$

$$R_{yy}[2] = \beta q$$

$$\boxed{R_{yy}[0] = q(1+\alpha^2+\beta^2), \quad R_{yy}[1] = q\alpha(1+\beta), \quad R_{yy}[2] = q\beta}$$

---

##### (ii) 当 $|m| > 2$ 时，求 $R_{yy}[m]$

**考查知识点**：
- MA过程的有限记忆性
- 自相关函数的截断特性

**解题步骤**：

**第一步**：分析 MA(2) 过程的结构
$y[n]$ 只依赖于 $x[n], x[n-1], x[n-2]$。

**第二步**：考虑 $|m| > 2$ 的情况
当 $m > 2$ 时，$y[n]$ 和 $y[n-m]$ 没有共同的 $x$ 样本：
- $y[n]$ 依赖于 $x[n], x[n-1], x[n-2]$
- $y[n-m]$ 依赖于 $x[n-m], x[n-m-1], x[n-m-2]$

由于 $m > 2$，这两组下标不重叠。

**第三步**：结论
由白噪声的不相关性：

$$\boxed{R_{yy}[m] = 0 \quad \text{for } |m| > 2}$$

---

##### (iii) 求 $S_{yy}(\omega)$

**考查知识点**：
- 离散时间系统的频率响应
- 功率谱密度的计算
- 实函数的证明

**解题步骤**：

**第一步**：写出系统传递函数
$$H(z) = 1 + \alpha z^{-1} + \beta z^{-2}$$

**第二步**：计算频率响应
$$H(e^{j\omega}) = 1 + \alpha e^{-j\omega} + \beta e^{-2j\omega}$$

**第三步**：计算功率谱
$$S_{yy}(\omega) = |H(e^{j\omega})|^2 S_{xx}(\omega) = |H(e^{j\omega})|^2 \cdot q$$

**第四步**：计算 $|H(e^{j\omega})|^2$
$$|H(e^{j\omega})|^2 = H(e^{j\omega}) \cdot H(e^{-j\omega})$$

$$= (1 + \alpha e^{-j\omega} + \beta e^{-2j\omega})(1 + \alpha e^{j\omega} + \beta e^{2j\omega})$$

**第五步**：展开并化简
展开得到：
$$= 1 + \alpha^2 + \beta^2 + \alpha(e^{j\omega} + e^{-j\omega}) + \alpha\beta(e^{j\omega} + e^{-j\omega}) + \beta(e^{2j\omega} + e^{-2j\omega})$$

利用 $e^{j\theta} + e^{-j\theta} = 2\cos\theta$：
$$= 1 + \alpha^2 + \beta^2 + 2\alpha\cos\omega + 2\alpha\beta\cos\omega + 2\beta\cos 2\omega$$

$$= 1 + \alpha^2 + \beta^2 + 2\alpha(1+\beta)\cos\omega + 2\beta\cos 2\omega$$

**第六步**：最终结果

$$\boxed{S_{yy}(\omega) = q[1 + \alpha^2 + \beta^2 + 2\alpha(1+\beta)\cos\omega + 2\beta\cos 2\omega]}$$

**证明是实函数**：表达式只包含 $\cos$ 项，而 $\cos$ 是实值函数，所以 $S_{yy}(\omega)$ 是实函数。

---

### 4(b) 马尔可夫链 (咖啡店选择)

#### 原题

Prof. Coffee Lover 每天选择咖啡店：Starbucks (S)、Local Cafe (L)、或 Home (H)。

- 从 S 出发：下次去 L 的概率 0.3，去 H 的概率 0.1
- 从 L 出发：下次去 S 的概率 0.5，去 H 的概率 0.2
- 从 H 出发：下次去 S 的概率 0.6，去 L 的概率 0.3

(i) 建立离散时间马尔可夫链，画状态转移图并写出一步转移矩阵。
(ii) 求稳态概率分布 $[\pi_S, \pi_L, \pi_H]$。
(iii) 若他从 Starbucks 开始，求两天后在 Home 的概率。

---

#### 详细解答

##### (i) 转移矩阵

**考查知识点**：
- 马尔可夫链的状态转移矩阵
- 概率的归一化

**解题步骤**：

**第一步**：确定所有转移概率
从 S：$P_{SS} = 1 - 0.3 - 0.1 = 0.6$, $P_{SL} = 0.3$, $P_{SH} = 0.1$
从 L：$P_{LS} = 0.5$, $P_{LL} = 1 - 0.5 - 0.2 = 0.3$, $P_{LH} = 0.2$
从 H：$P_{HS} = 0.6$, $P_{HL} = 0.3$, $P_{HH} = 1 - 0.6 - 0.3 = 0.1$

**第二步**：写出转移矩阵

$$\boxed{\Pi = \begin{bmatrix}
0.6 & 0.3 & 0.1 \\
0.5 & 0.3 & 0.2 \\
0.6 & 0.3 & 0.1
\end{bmatrix}}$$

（行：从 S, L, H；列：到 S, L, H）

---

##### (ii) 稳态概率分布

**考查知识点**：
- 稳态方程 $\pi \Pi = \pi$
- 概率归一化条件
- 线性方程组的求解

**解题步骤**：

**第一步**：建立稳态方程
$$\pi_S = 0.6\pi_S + 0.5\pi_L + 0.6\pi_H \quad (1)$$
$$\pi_L = 0.3\pi_S + 0.3\pi_L + 0.3\pi_H \quad (2)$$
$$\pi_H = 0.1\pi_S + 0.2\pi_L + 0.1\pi_H \quad (3)$$
$$\pi_S + \pi_L + \pi_H = 1 \quad (4)$$

**第二步**：从方程(2)简化
$$\pi_L = 0.3(\pi_S + \pi_L + \pi_H) = 0.3 \times 1 = 0.3$$

$$\boxed{\pi_L = 0.3}$$

**第三步**：从方程(3)求解
$$0.9\pi_H = 0.1\pi_S + 0.2 \times 0.3 = 0.1\pi_S + 0.06$$

$$\pi_H = \frac{0.1\pi_S + 0.06}{0.9} \quad (5)$$

**第四步**：利用方程(4)
$$\pi_S + 0.3 + \pi_H = 1$$
$$\pi_S + \pi_H = 0.7 \quad (6)$$

**第五步**：联立(5)和(6)求解
将(5)代入(6)：
$$\pi_S + \frac{0.1\pi_S + 0.06}{0.9} = 0.7$$

$$0.9\pi_S + 0.1\pi_S + 0.06 = 0.63$$

$$\pi_S = \frac{0.57}{1} = 0.57$$

等等，让我重新计算：
$$0.9\pi_S + 0.1\pi_S + 0.06 = 0.9 \times 0.7 = 0.63$$
$$\pi_S = 0.57$$

$$\pi_H = 0.7 - 0.57 = 0.13$$

验证：$0.57 + 0.3 + 0.13 = 1.0$ ✓

$$\boxed{[\pi_S, \pi_L, \pi_H] \approx [0.57, 0.30, 0.13]}$$

---

##### (iii) 两天后在 Home 的概率

**考查知识点**：
- 多步转移概率
- 矩阵乘法

**解题步骤**：

**第一步**：计算 $\Pi^2$
$$\Pi^2 = \Pi \cdot \Pi$$

**第二步**：计算 $(\Pi^2)_{13}$（从S到H经过2步）
$$(\Pi^2)_{13} = \sum_{k} \Pi_{1k} \cdot \Pi_{k3}$$

$$= 0.6 \times 0.1 + 0.3 \times 0.2 + 0.1 \times 0.1$$

$$= 0.06 + 0.06 + 0.01 = 0.13$$

$$\boxed{P(\text{Home after 2 days} | \text{start from S}) = 0.13}$$

---

## 知识点总结

### 本卷涉及的主要知识点

1. **非平稳过程**
   - 增量过程的性质
   - 维纳过程（布朗运动）
   - 自相关函数的计算

2. **WSS过程与LTI系统**
   - 频率响应的计算
   - 功率谱与频率响应的关系
   - 互相关函数的性质

3. **MA过程**
   - 移动平均过程的自相关
   - 功率谱的计算
   - 有限记忆特性

4. **马尔可夫链**
   - 状态转移矩阵
   - 稳态分布的求解
   - 多步转移概率

---

**难度评估**：★★★☆☆ (中等)

**重点题目**：3(a) 股票价格模型 - 典型的维纳过程推导
