# 随机过程模拟卷 B - 详细解答

---

## 第3题 (WSS过程与泊松过程)

### 3(a) 温度传感器网络

#### 原题

有两个温度传感器测量同一区域。设 $x(t)$ 和 $y(t)$ 为联合 WSS 实过程。
传感器融合输出为：
$$z(t) = ax(t) + by(t)$$
其中 $a, b$ 为常数。

已知：
- $R_{xx}(\tau) = 4e^{-|\tau|}$
- $R_{yy}(\tau) = 9e^{-2|\tau|}$
- $R_{xy}(\tau) = 3e^{-|\tau|}$

(i) 求 $R_{zz}(\tau)$（用 $a, b$ 表示）。
(ii) 若要使 $R_{zz}(0)$ 最小且满足 $a + b = 1$，求最优的 $a, b$。
(iii) 在最优条件下，判断 $z(t)$ 是否为 WSS。

---

#### 详细解答

##### (i) 求 $R_{zz}(\tau)$

**考查知识点**：
- 联合WSS过程的自相关
- 互相关函数的性质
- 线性组合的自相关计算

**解题步骤**：

**第一步**：写出自相关函数的定义
$$R_{zz}(\tau) = E\{z(t+\tau)z(t)\}$$

**第二步**：代入 $z(t) = ax(t) + by(t)$
$$= E\{[ax(t+\tau)+by(t+\tau)][ax(t)+by(t)]\}$$

**第三步**：展开
$$= a^2 E\{x(t+\tau)x(t)\} + ab E\{x(t+\tau)y(t)\}$$
$$+ ab E\{y(t+\tau)x(t)\} + b^2 E\{y(t+\tau)y(t)\}$$

**第四步**：用自相关和互相关表示
$$= a^2 R_{xx}(\tau) + ab R_{xy}(\tau) + ab R_{yx}(\tau) + b^2 R_{yy}(\tau)$$

**第五步**：处理 $R_{yx}(\tau)$
对于实过程：$R_{yx}(\tau) = R_{xy}(-\tau)$

对于给定的 $R_{xy}(\tau) = 3e^{-|\tau|}$：
$$R_{xy}(-\tau) = 3e^{-|-\tau|} = 3e^{-|\tau|}$$

所以 $R_{xy}(\tau) + R_{yx}(\tau) = 6e^{-|\tau|}$

**第六步**：代入数值
$$R_{zz}(\tau) = 4a^2 e^{-|\tau|} + 6ab e^{-|\tau|} + 9b^2 e^{-2|\tau|}$$

$$\boxed{R_{zz}(\tau) = (4a^2 + 6ab)e^{-|\tau|} + 9b^2 e^{-2|\tau|}}$$

---

##### (ii) 最优 $a, b$

**考查知识点**：
- 带约束的优化问题
- 拉格朗日乘数法（或代入法）
- 导数求极值

**解题步骤**：

**第一步**：计算 $R_{zz}(0)$
$$R_{zz}(0) = 4a^2 + 6ab + 9b^2$$

**第二步**：利用约束条件 $a + b = 1$
代入 $a = 1 - b$：

$$f(b) = 4(1-b)^2 + 6(1-b)b + 9b^2$$

**第三步**：展开
$$= 4(1 - 2b + b^2) + 6b - 6b^2 + 9b^2$$
$$= 4 - 8b + 4b^2 + 6b - 6b^2 + 9b^2$$
$$= 4 - 2b + 7b^2$$

**第四步**：求导并令其为零
$$f'(b) = -2 + 14b = 0$$

$$b = \frac{2}{14} = \frac{1}{7}$$

$$a = 1 - \frac{1}{7} = \frac{6}{7}$$

**第五步**：验证是最小值
$$f''(b) = 14 > 0$$（是最小值点）

$$\boxed{a = \frac{6}{7}, \quad b = \frac{1}{7}}$$

**物理意义**：由于传感器 $x(t)$ 的噪声方差较小（$R_{xx}(0)=4$ vs $R_{yy}(0)=9$），最优融合给予 $x(t)$ 更大的权重。

---

##### (iii) 判断 WSS

**考查知识点**：
- WSS的定义条件
- 联合WSS过程的线性组合

**解题步骤**：

**第一步**：检验 WSS 条件

WSS 要求：
1. $E\{z(t)\}$ 为常数
2. $R_{zz}(t_1, t_2)$ 只依赖于 $\tau = t_1 - t_2$

**第二步**：验证均值
$$E\{z(t)\} = aE\{x(t)\} + bE\{y(t)\}$$

由于 $x(t)$ 和 $y(t)$ 是 WSS，其均值为常数，所以 $E\{z(t)\}$ 也是常数。

**第三步**：验证自相关
由(i)可知，$R_{zz}(\tau)$ 只依赖于 $\tau$，不依赖于绝对时间。

**第四步**：结论

$$\boxed{z(t) \text{ 是 WSS}}$$

**理由**：联合 WSS 过程的线性组合仍为 WSS。

---

### 3(b) 泊松过程 (外卖订单)

#### 原题

外卖订单到达数 $N(t)$ 是泊松过程，已知午餐时段（12:00-13:00）平均到达 30 个订单。

(i) 求 20 分钟内恰好到达 8 个订单的概率。
(ii) 求 30 分钟内到达订单数的方差。
(iii) 已知前 15 分钟到达了 6 个订单，求前 30 分钟总共到达不超过 12 个订单的概率。

---

#### 详细解答

##### (i) 20分钟内恰好8个订单

**考查知识点**：
- 泊松过程的定义
- 泊松分布的概率公式
- 到达率的计算

**解题步骤**：

**第一步**：计算到达率 $\lambda$
60分钟内平均30个订单：
$$\lambda \times 60 = 30$$
$$\lambda = 0.5 \text{ 个/分钟}$$

**第二步**：计算20分钟内的期望到达数
$$\mu = \lambda \times 20 = 0.5 \times 20 = 10$$

**第三步**：应用泊松分布公式
$$P\{N(20) = 8\} = \frac{e^{-\mu} \mu^8}{8!} = \frac{e^{-10} \times 10^8}{40320}$$

**第四步**：数值计算
$$= \frac{e^{-10} \times 100000000}{40320}$$
$$= e^{-10} \times 2480.16$$
$$\approx 4.54 \times 10^{-5} \times 2480.16$$
$$\approx 0.1126$$

$$\boxed{P\{N(20) = 8\} \approx 0.1126}$$

---

##### (ii) 30分钟内方差

**考查知识点**：
- 泊松过程的方差性质
- 方差等于均值

**解题步骤**：

**第一步**：回顾泊松过程的性质
对于泊松过程，$\text{Var}(N(t)) = E\{N(t)\} = \lambda t$

**第二步**：计算
$$\text{Var}(N(30)) = \lambda \times 30 = 0.5 \times 30 = 15$$

$$\boxed{\text{Var}(N(30)) = 15}$$

---

##### (iii) 条件概率

**考查知识点**：
- 泊松过程的独立增量性质
- 条件概率的计算

**解题步骤**：

**第一步**：利用独立增量性质
$$P\{N(30) \leq 12 | N(15) = 6\}$$

由独立增量性质：
$$= P\{N(30) - N(15) \leq 12 - 6\}$$
$$= P\{N(30) - N(15) \leq 6\}$$

**第二步**：确定 $N(30) - N(15)$ 的分布
$N(30) - N(15) \sim \text{Poisson}(\lambda \times 15) = \text{Poisson}(7.5)$

**第三步**：计算累积概率
$$P\{N(30) - N(15) \leq 6\} = \sum_{k=0}^{6} \frac{e^{-7.5}(7.5)^k}{k!}$$

**第四步**：数值计算
$$= e^{-7.5}(1 + 7.5 + 28.125 + 70.31 + 131.84 + 197.75 + 247.19)$$
$$= e^{-7.5} \times 683.72$$
$$\approx 0.000553 \times 683.72$$
$$\approx 0.378$$

$$\boxed{P \approx 0.378}$$

---

### 3(c) AR(1) 频域分析

#### 原题

实平稳白噪声 $w[n]$，功率谱 $S_{ww}(\omega) = 3$。通过系统：
$$H(z) = \frac{1}{1 - 0.8z^{-1}}$$

(i) 求输出 $y[n]$ 的功率谱 $S_{yy}(\omega)$。
(ii) 求 $R_{yy}[0]$。
(iii) 设计一个白化滤波器 $G(z)$，使得 $y[n]$ 通过 $G(z)$ 后变为白噪声。

---

#### 详细解答

##### (i) 求 $S_{yy}(\omega)$

**考查知识点**：
- AR过程的功率谱
- 频率响应的模平方

**解题步骤**：

**第一步**：计算频率响应的模平方
$$|H(e^{j\omega})|^2 = \frac{1}{|1 - 0.8e^{-j\omega}|^2}$$

**第二步**：展开分母
$$|1 - 0.8e^{-j\omega}|^2 = (1 - 0.8e^{-j\omega})(1 - 0.8e^{j\omega})$$
$$= 1 - 0.8(e^{j\omega} + e^{-j\omega}) + 0.64$$
$$= 1.64 - 1.6\cos\omega$$

**第三步**：写出功率谱
$$S_{yy}(\omega) = |H(e^{j\omega})|^2 S_{ww}(\omega)$$

$$\boxed{S_{yy}(\omega) = \frac{3}{1.64 - 1.6\cos\omega}}$$

---

##### (ii) 求 $R_{yy}[0]$

**考查知识点**：
- AR(1)过程的自相关
- 功率谱与自相关的关系

**解题步骤**：

**方法一**：利用 AR(1) 公式

对于 AR(1) 过程 $y[n] = ay[n-1] + w[n]$：
$$R_{yy}[0] = \frac{\sigma_w^2}{1 - a^2}$$

其中 $a = 0.8$，$\sigma_w^2 = 3$：
$$R_{yy}[0] = \frac{3}{1 - 0.64} = \frac{3}{0.36} = 8.33$$

**方法二**：积分验证
$$R_{yy}[0] = \frac{1}{2\pi} \int_{-\pi}^{\pi} S_{yy}(\omega) d\omega$$

$$\boxed{R_{yy}[0] = \frac{25}{3} \approx 8.33}$$

---

##### (iii) 白化滤波器

**考查知识点**：
- 白化滤波器的设计
- 逆系统的概念

**解题步骤**：

**第一步**：理解白化滤波器
白化滤波器 $G(z)$ 使得输出功率谱为常数（白噪声）。

**第二步**：设计原理
$G(z) = H^{-1}(z)$（$H(z)$ 的逆系统）

**第三步**：计算
$$G(z) = \frac{1}{H(z)} = 1 - 0.8z^{-1}$$

$$\boxed{G(z) = 1 - 0.8z^{-1}}$$

**验证**：输出功率谱为
$$S_{out}(\omega) = |G(e^{j\omega})|^2 S_{yy}(\omega) = |1 - 0.8e^{-j\omega}|^2 \times \frac{3}{|1 - 0.8e^{-j\omega}|^2} = 3$$

确实是白噪声。

---

## 第4题 (遍历性与马尔可夫链)

### 4(a) 遍历性判定

#### 原题

实平稳零均值过程 $x(t)$，自协方差 $C_{xx}(\tau) = \frac{4}{1+\tau^2}$。

(i) 利用 Slutsky 定理判断 $x(t)$ 是否均值遍历 (mean-ergodic)。

(ii) 若已知：
$$E\{x(t+\lambda+\tau)x(t+\tau)x(t+\lambda)x(t)\} = \frac{16}{(1+\lambda^2)^2} \cdot h(\tau)$$
其中 $h(\tau) = 1 + \frac{1}{1+\tau^2}$。

判断 $x(t)$ 是否协方差遍历。

---

#### 详细解答

##### (i) 均值遍历性

**考查知识点**：
- Slutsky 定理
- 均值遍历的充分条件
- 反常积分的计算

**解题步骤**：

**第一步**：回顾 Slutsky 定理
若 $\lim_{T \to \infty} \frac{1}{T} \int_0^T C_{xx}(\tau) d\tau = 0$，则过程是均值遍历的。

**第二步**：计算积分
$$\frac{1}{T} \int_0^T \frac{4}{1+\tau^2} d\tau = \frac{4}{T} \left[\arctan(\tau)\right]_0^T = \frac{4\arctan(T)}{T}$$

**第三步**：计算极限
当 $T \to \infty$：
$$\lim_{T \to \infty} \frac{4\arctan(T)}{T} = \frac{4 \times \pi/2}{\infty} = 0$$

**第四步**：结论

$$\boxed{x(t) \text{ 是均值遍历的}}$$

---

##### (ii) 协方差遍历性

**考查知识点**：
- 协方差遍历的判定条件
- 四阶矩的处理

**解题步骤**：

**第一步**：定义辅助过程
$$z(t) = x(t+\lambda)x(t) - R_{xx}(\lambda)$$

协方差遍历要求 $z(t)$ 是均值遍历的。

**第二步**：计算 $z(t)$ 的自协方差
$$C_{zz}(\tau) = E\{z(t+\tau)z(t)\} - E^2\{z(t)\}$$

由于 $E\{z(t)\} = R_{xx}(\lambda) - R_{xx}(\lambda) = 0$：
$$C_{zz}(\tau) = E\{x(t+\lambda+\tau)x(t+\tau)x(t+\lambda)x(t)\} - R_{xx}^2(\lambda)$$

**第三步**：代入给定条件
$$= \frac{16}{(1+\lambda^2)^2} \cdot h(\tau) - \left(\frac{4}{1+\lambda^2}\right)^2$$

$$= \frac{16}{(1+\lambda^2)^2} \left(h(\tau) - 1\right)$$

$$= \frac{16}{(1+\lambda^2)^2} \cdot \frac{1}{1+\tau^2}$$

**第四步**：检验 Slutsky 条件
$$\frac{1}{T} \int_0^T C_{zz}(\tau) d\tau = \frac{16}{(1+\lambda^2)^2} \cdot \frac{\arctan(T)}{T} \to 0$$

当 $T \to \infty$。

**第五步**：结论

$$\boxed{x(t) \text{ 是协方差遍历的}}$$

---

### 4(b) 马尔可夫链 (电梯调度)

#### 原题

一栋三层楼的电梯，状态为当前所在楼层：1, 2, 3。
每次移动的转移规则如下：
- 从楼层 1：上到 2 的概率 0.7，直接到 3 的概率 0.1
- 从楼层 2：下到 1 的概率 0.3，上到 3 的概率 0.4
- 从楼层 3：下到 2 的概率 0.5，直接到 1 的概率 0.2

(i) 画状态转移图并写出转移矩阵 $\Pi[1]$。
(ii) 求稳态概率分布。
(iii) 电梯从楼层 1 开始，求经过 2 步后回到楼层 1 的概率。
(iv) 若楼层 2 是电费最高的位置，求长期平均电费占比。

---

#### 详细解答

##### (i) 转移矩阵

**考查知识点**：
- 状态转移矩阵的构建
- 概率归一化

**解题步骤**：

**第一步**：计算对角线概率（停留概率）
- 从1：$P_{11} = 1 - 0.7 - 0.1 = 0.2$
- 从2：$P_{22} = 1 - 0.3 - 0.4 = 0.3$
- 从3：$P_{33} = 1 - 0.5 - 0.2 = 0.3$

**第二步**：写出转移矩阵

$$\boxed{\Pi = \begin{bmatrix}
0.2 & 0.7 & 0.1 \\
0.3 & 0.3 & 0.4 \\
0.2 & 0.5 & 0.3
\end{bmatrix}}$$

---

##### (ii) 稳态概率分布

**考查知识点**：
- 稳态方程的建立与求解

**解题步骤**：

**第一步**：建立方程组
$$\pi_1 = 0.2\pi_1 + 0.3\pi_2 + 0.2\pi_3$$
$$\pi_2 = 0.7\pi_1 + 0.3\pi_2 + 0.5\pi_3$$
$$\pi_3 = 0.1\pi_1 + 0.4\pi_2 + 0.3\pi_3$$
$$\pi_1 + \pi_2 + \pi_3 = 1$$

**第二步**：简化方程
从第一个方程：
$$0.8\pi_1 = 0.3\pi_2 + 0.2\pi_3 \quad (1)$$

从第三个方程：
$$0.7\pi_3 = 0.1\pi_1 + 0.4\pi_2 \quad (2)$$

**第三步**：求解
设 $\pi_1 = x$，从(1)和(2)及归一化条件求解。

经过计算：
$$\pi_1 \approx 0.24, \quad \pi_2 \approx 0.47, \quad \pi_3 \approx 0.29$$

$$\boxed{[\pi_1, \pi_2, \pi_3] \approx [0.24, 0.47, 0.29]}$$

---

##### (iii) 两步后回到楼层1

**考查知识点**：
- 多步转移概率
- 矩阵乘法

**解题步骤**：

**第一步**：计算 $(\Pi^2)_{11}$
$$(\Pi^2)_{11} = \sum_{k=1}^{3} \Pi_{1k} \cdot \Pi_{k1}$$

$$= 0.2 \times 0.2 + 0.7 \times 0.3 + 0.1 \times 0.2$$
$$= 0.04 + 0.21 + 0.02 = 0.27$$

$$\boxed{P = 0.27}$$

---

##### (iv) 长期电费占比

**考查知识点**：
- 稳态概率的应用
- 长期平均行为

**解题步骤**：

长期处于楼层2的概率就是 $\pi_2$：

$$\boxed{\pi_2 \approx 0.47 = 47\%}$$

---

## 知识点总结

### 本卷涉及的主要知识点

1. **联合WSS过程**
   - 线性组合的自相关
   - 互相关函数的性质
   - 最优融合权重

2. **泊松过程**
   - 泊松分布的概率计算
   - 独立增量性质
   - 条件概率

3. **AR过程**
   - 功率谱计算
   - 白化滤波器设计
   - 自相关函数

4. **遍历性**
   - Slutsky定理
   - 均值遍历与协方差遍历
   - 四阶矩处理

5. **马尔可夫链**
   - 状态转移矩阵
   - 稳态分布
   - 多步转移概率

---

**难度评估**：★★★★☆ (中等偏难)

**重点题目**：4(a) 遍历性判定 - 需要理解四阶矩条件
